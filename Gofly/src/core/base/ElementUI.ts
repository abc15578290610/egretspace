module game {
	/**UI界面元素 */
	export class ElementUI extends BaseUI{
		public constructor() {
			super();
		}
		/**所有子组件初始化完成 */
		protected init(){
			super.init();
		}
		/**销毁方法 */
		protected destroy(){
			super.destroy();
		}
	}
}